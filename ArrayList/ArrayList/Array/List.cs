// var numbers = new List<int> {1, 2, 3, 4};
// foreach (var number in numbers)
// {
//     Console.WriteLine(number);
// }
//
// numbers.Add(1);
// numbers.AddRange(new[] {1, 2, 3});
//
// numbers.Remove(1);
//
// foreach (var number in numbers)
// {
//     Console.WriteLine(number);
// }
//
// numbers.Remove(numbers.LastIndexOf(1));
//
// foreach (var number in numbers)
// {
//     Console.WriteLine(number);
// }